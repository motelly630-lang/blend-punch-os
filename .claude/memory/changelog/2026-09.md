# 2026-09 변경 이력

무엇을 언제 왜 고쳤는지. **append-only** — 기존 줄을 고치지 않고 아래에 덧붙인다.
커밋 해시가 있는 항목은 `git show <해시>` 로 전문을 볼 수 있다(이 파일에 diff 를 옮겨 적지 않는다).

형식: `날짜 · 커밋 · 무엇을 / 왜 / 어떻게 확인했는지`

---

## 2026-09-24

- **인플루언서 등록 화면 간소화** (`3442a3b`, 운영 `92e6277 → 3442a3b`)
  왜: 섹션 6개로 길어 등록이 번거로웠다 (Ideas_MOC "인플루언서 등록 간소화").
  무엇: 주소 가져오기 → 기본(사진·이름·카테고리) → 연락처 → 정산(유형 선택 후 해당 칸+계좌) → 메모/추가 정보(접힘).
  칸 이름·서버 로직은 그대로, 안 쓰는 칸은 삭제 대신 접음(편집 저장 시 기존 값 보존).
  버그 수정: 카테고리가 있으면 `tojson` 큰따옴표가 `x-data` 속성을 끊어 **편집 화면이 깨지던 기존 버그** (`forceescape`).
  확인: 테스트 89 통과(신규 3), 격리 서버 헤드리스 크롬 스크린샷(등록·편집), 백업 `deploy-backup-20260923-232449.tar.gz`,
  import 사전검사 통과, `active`, `/login` 200, 운영 `app.css` 해시 로컬과 일치. 로그인 화면 클릭 확인은 대표님.
  **후속 핫픽스** (`29c455c`, 운영 `→ 29c455c`): 폼 action 슬래시 누락으로 등록 시 `POST /influencersnew` 404 — 대표님 실사용에서 발견.
  테스트는 저장 주소로 직접 POST 해서 못 잡았다 → 폼 action 검사 테스트 추가(버그 버전에서 실패 확인).

- **학습 반영 체계 도입 (`/os-learn`)** — 대표님 지시([[PF-003]]). 대화·작업에서 확인된 교훈을 성격별로
  스킬/지침/지식/에이전트에 조건부로 반영하고 원장(`learning/LEDGER.md`)에 이유·이전 상태·되돌리기·평가를 남긴다.
  무엇: `os-learn` 스킬, `learn.py`(pending·lint·metrics), SessionStart 에 "반영 대기 세션" 한 줄, 평가 사례 EV-001.
  첫 적용 IM-001~009: gen-test·quality-gate(전역, 백업 `~/.claude/backups/learn/2026-09-24/`)에 폼 제출주소 검사,
  RG-006 + `check_tojson_attr.py`(검사기) → **IS-006 제품 수정 화면 등 8곳 발견**(수정 대기),
  낡은 "테스트 없음" 기록 정정, 메모리 라우터가 `prompt` 필드도 읽게(실제 필드명 미확인).
  확인: 각 selftest, `learn.py lint` error 0, 테스트 90 통과, 검사기가 고치기 전 폼에서 위반 검출.

- **템플릿 JSON 속성 깨짐 11곳 수정** (IS-006 resolved) — 제품 수정·제품 상세·CS 상세·쇼핑몰 제품·
  인플루언서/제품 엑셀 가져오기 미리보기. 검사기 빈틈 2개(여러 건 중 마지막만 · `| e` 를 안전으로 오판) 수정 후 발견.
  확인: `tests/test_template_json_attrs.py` 4건 고치기 전 FAIL → 고친 뒤 OK, 전체 94 통과. 폼 주소 ↔ 라우트 전수 검사 실문제 0.
  배움 카드 규칙 추가(대표님 요청): 배운 게 생기면 연결·보완·강화 + "만들어도 될까요?" 로 먼저 알림.

- **템플릿 자동 점검 훅** (IM-012, 대표님 승인) — 템플릿 저장 즉시 `tojson` 속성 깨짐 + 폼/htmx 주소 404 를 검사
  (`os-template-check.py`, `check_form_urls.py`). 확인: 실제 세션에서 슬래시 누락을 저장하자 즉시 경고, 테스트 97 통과.
- **DE-007 OS 쇼핑몰(`/shop`) 미사용** — 대표님 결정. 코드는 유지, `dc3bfca` 이후 변경은 운영 미배포(대표님 "배포 무시").

- **OS 쇼핑몰(`/shop`) 삭제** (`43220bc`, DE-007) — 판매 화면·토스 결제확인 코드·`/shop` 링크. DB 데이터와 모델은 유지.
  근거: 블랜드픽이 OS `/shop` 을 부르지 않음(자체 토스 확인), 2주 기록에 정상 결제확인 0건. 확인: 테스트 96 통과, 관리 화면 5개 200,
  블랜드픽 문의 제출 주소 정상. **운영 미배포.**
- **블랜드픽·산지픽 ↔ OS 연결 지도** (PR-003) + 구조 변경 전 영향 확인 규칙 (RG-007, create-migration 에 연결).
  블랜드픽(Next.js, 같은 EC2)이 OS 운영 DB 를 직접 읽고 씀(인플루언서 추가·수정·삭제, 캠페인 수정, 회원) + 문의는 OS 주소 호출.

- **인플루언서 일괄 보강을 Meta 공식 조회로** (T4, DE-006) — `influencer_enrich` 가 Meta 사용, 사진만 빈 사람도 대상,
  사용량 50% 자동 멈춤, 한도·토큰 오류는 기록 없이 멈춤(다음 실행 재시도), 개인 계정만 30일 제외. `meta_instagram` 에 오류 종류·사용량.
  확인: 테스트 8건 신규·전체 통과, 로컬 DB 실제 5명(수집 4·조회불가 1, 사용량 1%), 저장 사진 JPEG 확인. **운영 미적용.**

- **쇼핑몰 삭제 되돌림** (`db6c18b`, DE-007) — 개발자와 논의 전 손대지 않기로. 운영 대비 쇼핑몰 변화는 따옴표 처리 2줄뿐.
- **운영 배포 `29c455c → 0a9e960`** — 인플루언서 일괄 보강(Meta)·템플릿 따옴표 수정 등. import 사전검사 통과, active, `/login` 200.
  **운영 수동 보강 20명:** 수집 18 · 조회불가(개인 계정) 2 · 58초 · Meta 사용량 5%. 사진 18장 공개 주소 200 확인. 남은 대기 1,114명.
  새벽 자동 작업(`INFLUENCER_ENRICH`)은 아직 끔.

- **운영 배포 `0a9e960 → 4de996d` + 인플루언서 자동 보강 켬** — 목록 '정보 상태' 필터, 복사 찌꺼기 아이디 정리,
  `reels` 등 예약어 아이디 조회 차단(운영 reels 2·인스타그램 1). 서버 `.env` 에 `INFLUENCER_ENRICH=true`·`_LIMIT=200`
  (백업 `.env.bak-20260924-034826`). 확인: 스케줄 로그 "influencer enrich 04:30 (200/day)", active, `/login` 200.
- **중복 점검 (운영, 읽기)**: 같은 아이디 32묶음·66명, URL 교차 4묶음, 이름만 같음 1묶음, 전화 0 — 합치기 미정(T9).
- **옵시디언**: Claw = 소싱 담당(`Agent_Genspark_Claw`, 받은편지함 `03_Seller_Brand/Sourcing_Inbox/`, 양식
  `Template_Sourcing_Product`), 자동 수집 Testing, 서버 정보(블랜드픽·산지픽 = 한 프로그램) 채움. 깨진 링크 0.

- **트렌드 요약 Slack 05 켬** — 서버 `SLACK_EVENTS` 에 `trend_digest` (백업 `.env.bak-20260924-041037`). AI 크레딧 없어 규칙 선택.
- **인플루언서 중복 합치기 도구** (`7a5b6f0`·`93aeaab` 배포) + **운영 실행**: 31묶음 합침·33줄 보관, 기록 이동 섭외 43·캠페인 4·정산 2,
  남은 중복 0. 독립 리뷰 6건 반영(모든 쌍 충돌 검사, 되돌리기 순서·덮어쓰기 방지, 블랙리스트, 블랜드픽 칸, 시트 이름검색 보관 제외, 표 이름).
  시트 번호가 둘 다 있으면 보관 줄이 번호 유지 → `/influencers/duplicates` 에 "시트에서 지울 줄" 33개(대표님: 사람이 삭제).
  실행 전 백업 `/home/ubuntu/merge-backup-20260924-042631.json` (600 권한, 계좌 등 포함 — 확인 후 삭제 필요).
- 발견: 블랜드픽이 OS `influencers` 표에 칸 15개 추가(신분증·통장·사업자 파일, 포털 비밀번호 등) → PR-003 반영.

- **Slack 점검 (읽기)**: OS 봇 `bp_os` 권한 = `chat:write` `chat:write.public` `channels:read` `groups:read` — 보내기·채널 목록만,
  **메시지 읽기 불가**. OS 채널 설정은 ID 로 되어 있어 5개 모두 정상(05 실제 이름 "05-마켓팅-메타"). 새 채널 `#07-제품-소싱`(OS 미연결).
  소싱: 젠스파크 맥 앱에 Claw 없음 → Genspark 탭 조사 + **GenCode 가 옵시디언 Sourcing_Inbox 쓰기 성공**.

- **첫 소싱 흐름 완주**: 젠스파크(Genspark 조사 → GenCode 저장)로 내역서 6개 → Claude 점검(양식·규칙·OS 중복 0) →
  옵시디언 표 `03_Seller_Brand/소싱_내역.base`(전체·상태별·검토 대기·카드) → OS 봇으로 `#07-제품-소싱`(C0C41GHAGSJ) 결과 게시
  (`slack_notification_logs` event `sourcing_result`). 검토는 표의 상태 칸 또는 Claude 에게.

- **정산서 한 장 개편** (DE-008) — 계산 엔진 `settlement_calc` 하나로 통일(정산 화면·자동 정산·소싱·시트), 새 칸 6개(migrate.py),
  탭 ① 정산할 것 ② 지급 대기 ③ 지급 완료 ④ 손익 + 검색·월별(한국 시간), 발행/발행 취소/지급 규칙, 정산서 A4 인쇄·PDF,
  수수료율 자동(캠페인→제품→인플루언서), 서버 미리보기, 포털 지급 예정일, 시트 예정일·완료일. 독립 리뷰 6건 중 5건 반영.
  확인: 테스트 17 신규·전체 통과, 화면 스크린샷. 운영 영향: 작성중 48건 중 프리랜서 2건만 새 식에서 금액 변경(확인 후 발행). **운영 미배포.**

- **운영 배포 `93aeaab → 873d761`** — 정산서 개편 + 정산서 있는 캠페인 삭제 막기. **배포 전 운영 migrate** (새 migrate.py 를
  origin/master 에서 꺼내 실행 → 칸 6개, 정산 48+2건·금액 합계 그대로). 확인: 서버 안 임시 세션으로 정산 4탭·정산서 2건·캠페인 목록 200,
  오류 로그 0. 백업 `deploy-backup-20260924-070019.tar.gz`.

- **캠페인 목록 가독성 + 공구 아카이브** — 목록: 한 줄 한 카드·이름 크게(잘림 없음)·D-day 큰 배지(오늘 마감 빨강·3일 안 주황)·
  상단 "오늘 마감 / 3일 안 / 오늘 시작" 줄, 인플루언서 사진(없는 칸 이름을 가리켜 안 보이던 버그 수정).
  아카이브 `/campaigns/gallery`: 진행 중·예정·지난·전체, 카드마다 인스타 릴스·유튜브 embed(토큰 없이 공식 embed 주소),
  캠페인 새 칸 `content_urls` JSON(칸 추가만 — RG-007 무영향, migrate). 상세·카드에서 링크 추가/빼기. 테스트 4 · 스크린샷(실제 릴스 재생 화면).
  **운영 미배포 (migrate 필요).**

- **운영 배포 `873d761 → c9ea9b4`** — 캠페인 목록 가독성 + 공구 아카이브. 배포 전 운영 migrate(`campaigns.content_urls`), 캠페인 156건 그대로.
  확인: 서버 안 임시 세션으로 목록·보관함·아카이브(진행·지난)·상세·정산 200, 오류 0. 백업 `deploy-backup-20260924-073330.tar.gz`.
- **공구 한 번에 등록** `/campaigns/quick` — 인스타 주소·제품·기간만으로 인플루언서(같은 아이디면 재사용, 새면 Meta 로 채움)·
  제품(없으면 '미분류'·브랜드 미입력 → 작성 필요, 제품 목록 '미완료')·브랜드(없으면 이름만 → 브랜드 목록 '정보 입력 필요')·캠페인(기간으로 상태).
  테스트 7(products.brand NOT NULL 버그를 테스트가 잡아 '브랜드 미입력' 처리). 스키마 변경 없음. **운영 미배포.**

## 2026-09-23

- `83aefe1` **캠페인 진행 처리 L2 전환 + 아침 보고에 처리 대기 추가** (미배포)
  왜: 커밋본(=운영)은 `GET /campaigns` 를 열 때마다 날짜만 보고 상태 변경·보관·정산 생성까지 했다.
  "처음엔 전부 L2(시스템 제안 → 사람 승인)" 로 결정. 승인 화면은 사람이 들어가야 보이므로
  아침 슬랙 보고에 처리 대기 건수 + `/campaigns/progress-review` 링크를 붙이고, 빈 날도 매일 보낸다
  (안 온 게 '없음'인지 '고장'인지 구분하려고).
  작업트리의 Codex WIP 중 캠페인분만 분리 커밋. `main.py`(rate_pct 필터)·`product_service.py`(추가분)는
  부분 반영, 에이전트 API·주문/결제·상품 화면은 작업트리에 남김.
  확인: HEAD + 캠페인분만 얹은 임시 worktree 와 스테이징 인덱스 각각에서 격리 테스트 15/15,
  `import app.main` 통과. 신규 템플릿 클래스는 기존 app.css 에 이미 포함(재빌드 불필요).
- (커밋 아님) **운영 슬랙 웹훅 연결** — EC2 `.env` 에 `ALERT_WEBHOOK_URL`·`ALERT_MOCK=false`·
  `CAMPAIGN_ALERT=true`·`CAMPAIGN_ALERT_HOUR=8`. 값은 서버에만 있다.
  확인: 테스트 메시지 슬랙 수신, 재시작 후 `Added job "_campaign_alert_job"`.
  주의: 첫 입력 때 URL 끝 4자가 잘려 `HTTP 403 invalid_token` — 마지막 경로 조각 길이(24)로 판별.
- **훅 등록을 상대경로로 전환 (맥북 공용화)** — `.claude/settings.json` 7곳
  `bash /home/blendpunch/blend-punch-os/.claude/hooks/...` → `bash .claude/hooks/...`.
  왜: 맥북에서도 같은 repo 로 작업하려면 WSL 절대경로가 없다. 문서상 훅 셸은 Windows(Git Bash 없음)=PowerShell,
  macOS=`sh -c` 라 `$CLAUDE_PROJECT_DIR` 같은 변수 문법은 공용이 안 되고(PS 에선 빈 값, 값도 UNC) 상대경로만 공용.
  확인: 프로젝트 루트(UNC)에서 `claude -p --include-hook-events` 실행 — SessionStart·UserPromptSubmit·
  PostToolUse:Write 3개 success exit=0, SessionEnd 는 state.json 갱신으로 확인(시험 흔적은 되돌림).
  제약: Claude Code 를 하위 폴더에서 열면 훅을 못 찾는다(exit 127 로 드러남). 맥북 실기기 검증은 아직.
- **운영 배포 `647b088` → `5206dd4`** (위 `83aefe1` 포함 4커밋, EC2 `git merge --ff-only`)
  사전점검: EC2 추적파일 변경 0 · 미추적 8개는 [[IS-004]] 잔재(신규 파일과 겹침 없음) · 비밀파일 미추적.
  백업 `/home/ubuntu/deploy-backup-20260923-065131.tar.gz`. 재시작 전 `import app.main` 통과.
  확인: `active`, 재시작 로그 오류 0, `_campaign_alert_job` 등록, `/login` 200.
  참고: 비로그인 `/campaigns` 가 로그인이 아니라 "비활성화된 기능" 으로 가는 건 기존 동작
  (FeatureGate 미들웨어가 인증보다 먼저 판정) — 이번 변경과 무관.
  실행 방식: 자동 권한 판정이 운영 SSH 쓰기를 막아 사용자가 `!` 로 배포 스크립트를 직접 실행.
- **pyproject 에 누락 의존성 3개 선언** — `slowapi`·`psycopg2-binary`·`xlrd` (+ `uv.lock` 갱신, 기존 버전 변경 0)
  왜: 맥북에서 새로 `uv sync` 하니 `import app.main` 이 `No module named 'slowapi'` 로 실패.
  WSL·EC2 venv 에는 수동 설치돼 있어 가려져 있었다. `openai` 는 어디에도 미설치·지연 import 라 현행 유지.
  확인: `git archive HEAD` + 수정 2파일로 빈 폴더에서 `uv sync --frozen --python 3.12` → 테스트 15/15,
  `app.main`·`psycopg2`·`xlrd` import 통과. WSL·EC2 venv 는 건드리지 않음(이미 설치됨).
- **DB MCP 인증서 오류 수리** — `.claude/mcp/os-db-launch.sh` 가 `NODE_EXTRA_CA_CERTS` 로 AWS RDS 공개 CA 번들
  (`.claude/mcp/rds-global-bundle.pem`, 108 certs, 비밀 아님)을 신뢰목록에 추가 + MCP 패키지 `@1.0.7` 고정.
  왜: `pg-connection-string` 2.14 가 `sslmode=require` 를 verify-full 로 해석 → RDS CA 를 몰라
  `self-signed certificate in certificate chain` 으로 os-db-prod 조회 실패. 검증을 끄는(no-verify) 대신 CA 추가.
  확인: 일부러 틀린 비밀번호로 pg 접속 — 번들 없으면 `SELF_SIGNED_CERT_IN_CHAIN`, 있으면 `28P01`(인증 단계 도달
  = TLS 통과). openssl 로도 `Verify return code: 0`. 실제 MCP 조회는 세션 재연결 후 확인 필요.
  → `/mcp` 재연결 후 확인 완료: os-db-prod = `blendpunch_dev` · os-db-local = `blendpunch`, 둘 다 `claude_ro`, ssl=true.
- (커밋 아님) **맥북(macOS, arm64) 작업환경 구성** — `~/blend-punch-os` clone(gh + SSH), `uv sync --python 3.12`,
  `npm install`, 테스트 15/15. 비밀파일 `.env`·`google_sa.json`·`~/.claude/os-db-ro.env` 는 LocalSend(LAN 직접)로
  이동 후 600. `.env` 의 `GOOGLE_SA_JSON` 은 WSL 절대경로라 맥 경로로 교체. DB MCP 2개 user scope 등록.
  확인: 훅 오류 없이 기동, 맥 Claude 가 CLAUDE.md·메모리 인식(DB 이름 구분·이슈 목록), MCP 조회 완료(사용자 보고).
  주의: 두 머신이 로컬 DB `blendpunch` 를 공유한다. 훅은 macOS 기본 python3(3.9)로 돈다 — 3.9 호환 확인함.
- **`deploy.sh` macOS 대응 + 안전 절차 내장** — IP 해석 `getent` → `dig` → `python3` 폴백,
  `check` 모드(읽기 전용 점검) 추가, `git` 모드에 오늘 수동으로 했던 절차를 내장
  (HEAD==origin 확인 · EC2 드리프트/ff/비밀파일 점검 · 백업 · ff-only · 재시작 전 import 검사·실패 시 원복).
  왜: 맥북에서도 배포하려면 스크립트가 Linux 전용 명령에 의존하면 안 되고, 안전 절차가 임시 파일에만 있었다.
  확인: `bash -n` 통과, `getent` 를 없앤 상태(export -f 로 127 반환)에서 `check` 실행 — python3 로 IP 해석,
  EC2 점검 정상(추적변경 0 · ff 가능 · 미배포 6커밋 · 비밀파일 미추적). `git` 모드는 다음 실제 배포 때 첫 검증.
- **운영 배포 `5206dd4` → `d42d832` (맥북에서, `deploy.sh git` 첫 실사용)** — 7커밋, 전부 문서·설정·스크립트
  (앱 동작 변경 없음). 백업 `/home/ubuntu/deploy-backup-20260923-095012.tar.gz`, import 사전검사 통과,
  재시작 로그 오류 0, `active`, `/login` 200. 맥북 배포 경로 검증 완료.

- **주민번호 저장 중단** (`b54db0f`, [[DE-005]]) — 폼·라우터·정산 상세에서 제거, 운영·로컬 기존 각 2건 NULL.
- **인스타 URL 가져오기 → Meta 공식 API** (`92e6277`, [[DE-006]], 운영 `b54db0f → 92e6277`)
  왜: 기존 경로(instagrapi 봇 로그인·AI 파싱)가 운영에서 동작하지 않아 아이디만 채워졌다(사진 111/1,260).
  무엇: `app/services/meta_instagram.py`(Business Discovery, 토큰 헤더 전송, CDN 한정·5MB 스트리밍 사진 저장),
  URL 가져오기 연결(이름은 비움), `main.py` 에서 httpx/httpcore 로그 WARNING 으로.
  확인: 독립 리뷰 — **httpx INFO 로그에 쿼리 속 토큰이 찍히는 경로** 재현·차단(헤더 전송 + 로그 레벨), 테스트 86 통과,
  운영에서 실제 조회 성공 + 배포 후 journal 토큰 흔적 0건. Meta 토큰 발급 중 1회 대화 노출 → 앱 연결 해제로 무효 확인(190/460).
  남은 것: 등록 화면 간소화, 기존 1,260명 일괄 보강(일괄 가져오기 `process_influencer_url` 은 아직 Meta 미사용).

- **Slack 자동화 운영 시작** (`f1802fb` `036235f` `d72cf5d`, 운영 `d42d832 → d72cf5d`)
  왜: Slack 채널 5개는 운영 중인데 OS → Slack 연결이 아침 웹훅 1개뿐이었다. 연휴 전 자동화 대량 구축.
  무엇: Slack 앱(BlendPunch OS, 봇 bp_os) 채널별 발송 기반(`slack_notify` — 이벤트별 켜기, 선점+부분 유니크
  인덱스 중복 방지, 별도 세션 기록, `& < >` 무력화), 02 아침 보고 Slack 이관·새 캠페인·일정 변경(10분 스캔,
  첫 스캔 기준점만), 03 새 브랜드·어제 등록 제품, 01 월요일 주간 요약, 대표님 DM 시스템 이상,
  05 AI 트렌드·02 AI 우선순위(구축만, 꺼둠). 신규 테이블 `slack_notification_logs`.
  서버 `.env` 에 `SLACK_BOT_TOKEN`·`SLACK_CHANNELS`·`SLACK_DM_USER`·`SLACK_EVENTS`(7종) — 값은 서버에만.
  백업 `deploy-backup-20260923-133151` · `-134047`, `.env.bak-slack-*` · `.env.bak-events-*`.
  확인: 독립 리뷰 3회(23건 반영), 테스트 68 통과, 운영 미리보기, 재시작 후 잡 등록 로그,
  첫 스캔 22:50 KST 기준점 26건(보관 130건 제외 전부)·발송 0·실패 0. 인덱스 `uq_slack_notif_dedupe_active` 생성 확인.
  주의: 토큰 1회 대화 노출 → 즉시 폐기·재발급. **Anthropic API 크레딧 소진** 확인 (OS AI 기능 전체 정지) —
  AI 알림은 충전 후 `SLACK_EVENTS` 에 `trend_digest,campaign_digest_ai` 추가. 정산 알림은 대표님 정산 재설계 후.
  다음 확인: 9/24 08:00 아침 보고가 BP OS 앱으로 1회만 오는지 (웹훅 이중 발송 없음).

## 2026-09-11

- `647b088` **버벅임의 진짜 원인 — 서비스 워커가 구 app.css 를 영구 캐시하고 있었다**
  왜: `8849ad2`(app.css 1.34MB → 263KB) 배포 후에도 "자산 > 제품 > 브랜드 누를 때마다 버벅인다"는
  보고가 계속됐다. 운영 재실측 결과 **서버·nginx 는 정상** — `/login` TTFB 36~50ms,
  `app.css` gzip 적용, `Cache-Control: public, max-age=3600`. 병목은 브라우저 쪽이었다.
  원인 ① `static/sw.js` 가 `/static/` 을 Cache-First 로 잡는데 `CACHE_NAME` 이 PWA 도입
  (`dacd42e`) 이후 `bpos-v1` 고정이었다. `activate` 는 **이름이 다른** 캐시만 지우므로,
  9/11 이전 방문자는 그때 캐시된 압축 전 1.34MB app.css(셀렉터 24,997개)를 영구히 썼고
  개선분이 전혀 전달되지 않았다. 무효화 수단 없는 Cache-First 는 nginx 헤더보다 나쁘다.
  원인 ② 모든 GET 을 `respondWith` 로 가로채 **요청마다 SW 웜업 비용이 선행**됐다.
  조치: `CACHE_NAME` → `bpos-v2`(activate 가 v1 삭제 = 구 CSS 제거가 핵심 목적) ·
  `/static/` 가로채기 제거(HTTP 캐시에 위임) · `req.mode !== 'navigate'` 는 통과 ·
  `navigationPreload` 활성화(SW 기동과 네트워크를 겹침) · `/sw.js` 에 `Cache-Control: no-cache`
  (같은 사고 재발 방지). SW 는 이제 **오프라인 폴백만** 담당한다.
  확인: `node --check` 통과, `import app.main` 통과, 배포 후 라이브 `/sw.js` 가 `bpos-v2` ·
  `startsWith('/static/')` 잔존 0건 · `cache-control: no-cache`, `systemctl is-active` active,
  `/login` 200 · `/public/products` 200 · `app.css` gzip 유지.
  ⚠️ 기존 사용자는 **다음 접속 1회**에 새 SW 가 설치되며 그때 v1 캐시가 삭제된다.

- **AI 작업환경 Phase 2 (자동화)** — Context Router · 수정 저널 · state 플러시 · 상태바 연동 · 체크포인트
  왜: Phase 1 로 상태가 디스크에 남게 됐으니, 이제 *찾아 쓰고 · 보이고 · 이어받는* 단계.
  - `os-mem-route.py`(UserPromptSubmit) — 요청과 관련 있는 메모리의 **포인터만** 주입(≤5건).
    의미 검색이 아니라 tags/title/id 어휘 일치다. 한국어는 조사 때문에 부분일치, 영문은 단어경계.
  - `os-mem-journal.py`(PostToolUse) — 수정 파일을 `{ts,tool,path}` 로만 append. LLM·승인 없음.
  - `os-mem-flush.py`(SessionEnd·PreCompact) — state 확정만. SessionEnd 1.5초 예산이라 요약 불가.
  - `os-hook.sh` — 훅 파이썬 공용 래퍼(스크립트마다 .sh 를 만들지 않는다).
  - 상태바 3번째 줄에 프로젝트·상태·작업·진행률·체크포인트. 다른 프로젝트에선 조용히 사라진다.
  - `/os-checkpoint` 스킬 — 위험 작업 전/검증 후 지점 기록, 다음 세션이 이어받기.
  확인: 각 훅 `--selftest`, 공용 래퍼로 실제 stdin JSON 호출, 상태바 4시나리오,
  기존 훅 4개(`os-build-css` `os-router-parity` `os-mem-load` `osmem`) 회귀 없음.

- `8849ad2` **UI 체감 성능 — 스타일시트 전송량 52배 감소**
  왜: 클릭할 때 버벅인다는 보고. 실측 결과 서버는 정상(TTFB 34~50ms, RDS 1.4ms, load 0.00)이고
  전송·렌더링이 병목이었다. 원인 둘 — ① tailwind safelist 정규식에 앵커가 없어 CSS 가 1.34MB
  (셀렉터 24,997개)로 부풂 ([[RG-005]]) ② nginx `gzip_types`/`gzip_proxied` 가 주석이라
  **아무것도 압축되지 않음** + `/static` 이 uvicorn 단일 워커를 거침 ([[DE-004]]).
  결과: `app.css` 1,404,821 바이트 → **27,008 바이트 전송**. HTML 8.7배, htmx 3배 감소.
  브라우저가 매 DOM 변경마다 대조하던 CSS 규칙이 24,997 → 2,929개로 줄었다.
  확인: 동적 조합 198개·정적 변형 8종·실렌더 클래스 166개 전수 대조 — 회귀 0건.
  `htmx` defer + `fonts.gstatic.com` preconnect 도 함께 적용.
  부수 발견: `static/logo.png` 이 원래 없었다 ([[IS-005]]).

- **EC2 배포키 등록 → `deploy.sh git` 정석 경로 복구** ([[IS-002]] 해소 완료)
  왜: EC2 배포키가 GitHub 에 없어 `git pull` 이 막혀 있었고, 배포마다 `git archive`/bundle 우회가 필요했다.
  확인: EC2 에서 `git fetch origin` → OK, `git pull --ff-only origin master` → `7cd863b..6820fcb`
  fast-forward 성공. HEAD = origin/master, 추적파일 수정 0건. 받아온 커밋에 앱 코드가 없어
  재시작 생략. 라이브 `/` 302 · `/login` 200 · `/public/products` 200.

- **노출됐던 classic PAT 폐기 확인** — `api.github.com/user` 인증 → **HTTP 401**.
  GitHub classic 토큰 목록에는 만료본 2개만 남아 있고 해당 토큰은 없다(만료본도 목록에 남으므로
  "목록에 없다 = 삭제됨"). 커밋 이력에 들어간 적 없어 히스토리 재작성 불필요 ([[IS-002]]).

- **origin push 8커밋 + EC2 git 트리 드리프트 해소** (`e562af4` → `7cd863b`)
  왜: 로컬이 origin 보다 8커밋 앞서 있었고, EC2 는 그동안 scp 배포 탓에 git 상태가 실제 배포 내용과
  어긋나 `git pull` 이 충돌했다 ([[IS-002]]).
  어떻게: EC2 배포키가 GitHub 에 미등록이라 `fetch` 가 막혀 **`git bundle` 로 우회**했다.
  사전검증(동일 359 / 다름 2 / 없음 67) + 백업 `pre-reset-20260911-133536.tar.gz` 후 `reset --hard`.
  확인: 추적파일 수정 113 → **0건**, `app/` 코드 차이 0건이라 재시작 불필요,
  `.env`·`google_sa.json` 생존, `systemctl is-active` active, 에러 0건, `/login` 200 · `/public/products` 200.
  부수 발견: EC2 에만 있는 미추적 파일 9개 — 전부 미참조 잔재로 확인 ([[IS-004]]).

- `e6e448b` **프로젝트 영구 메모리 토대 (Phase 1)**
  왜: 세션이 끝나면 맥락이 전부 사라지고, 같은 설명을 매번 반복하고, 과거에 고친 것이 다시
  깨져도 알아차릴 장치가 없었다. 확인: `osmem.py --selftest` 통과, 훅을 실제 stdin JSON 으로
  호출해 84줄 주입·state 기록 확인, 기존 훅 2개 셀프테스트 회귀 없음.
  남은 것: Phase 2~4 (Context Router, 상태바 연동, 체크포인트, regression 3계층, pytest, 스킬 lifecycle).

- `e757a93` **DB MCP 접속문자열에 `sslmode=require` 부착**
  왜: RDS 가 SSL 을 강제하는데 sslmode 가 없어 `os-db-local`/`os-db-prod` 가 쿼리 시
  `no pg_hba.conf entry ... no encryption` 으로 거부당했다. 자격증명 파일이 아니라 런처에서 붙였다.
  확인: 두 DB 읽기 연결 성공 (`blendpunch` / `blendpunch_dev`, 47 테이블, `claude_ro`).

- (커밋 아님) **Windows 상태줄 추가** — `~/.claude/statusline.js` + `~/.claude/settings.json`.
  컨텍스트 사용률·요금제 사용량(5시간/7일)·비용을 한국어로 표시. repo 밖 개인 설정이라 미커밋.
  왜: 토큰을 얼마나 썼는지 바로 보이게. 확인: 4가지 입력(정상/집계 전/한도 임박/빈 입력) 렌더 확인.

- (커밋 아님) **`claude-mem` 플러그인 비활성화** — `~/.claude/settings.json`.
  왜: 데이터가 0건인 휴면 상태인데 `SessionStart`·`UserPromptSubmit`·`PostToolUse`·`Stop` 훅을
  등록해 신규 메모리 훅과 이벤트가 겹치고, 되살아나면 세션마다 observation 50건을 무제한 주입한다.
  되돌리려면 `enabledPlugins` 값을 `true` 로.

## 2026-09-10

- **운영 배포 18개 파일 + 데이터 정규화** (커밋 없이 `git archive HEAD` 전송 — [[DE-002]])
  왜: 테넌트 격리 수정 5개 라우터분과 스냅샷 커밋분이 운영에 반영돼 있지 않았다.
  확인: blob SHA 18/18 일치, `import app.main` 사전검사 통과, `migrate.py` 실행
  (`products.visibility_status '공개' → 'active'` 4건 — [[RG-003]]), 재시작 후 err 로그 0건,
  `/login` 200, `/public/products` 200(제품 39건).
- **로컬 git remote 를 SSH 로 전환** — `.git/config` 에 평문으로 박혀 있던 classic PAT 제거.
  GitHub 쪽 revoke 와 deploy key 등록은 **미완료** ([[IS-002]]).
