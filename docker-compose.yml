services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./blendpunch.db:/app/blendpunch.db
      - ./backups:/app/backups
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
